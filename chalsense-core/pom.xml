<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.chalsense</groupId>
        <artifactId>chalsense-parent</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>chalsense-core</artifactId>
    <name>ChalSense Core</name>
    <description>Framework-independent ChalSense verification core.</description>

    <dependencies>
        <dependency>
            <groupId>io.github.chalsense</groupId>
            <artifactId>chalsense-protocol</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <testResources>
            <testResource>
                <directory>${project.basedir}/../docs/test-vectors</directory>
                <includes>
                    <include>coordinates-v1.json</include>
                    <include>protocol-v1.json</include>
                    <include>challenge-create-v1.json</include>
                    <include>state-json-v1.json</include>
                    <include>attack-baseline-v1.json</include>
                </includes>
            </testResource>
        </testResources>
    </build>
</project>
