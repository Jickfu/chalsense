# AGENTS.md

本文件定义 ChalSense 仓库的通用协作规则。

## 沟通与上下文

- 使用简体中文沟通，代码标识符、协议字段和公开 API 使用清晰的英文。
- 开始设计或实现前，按根目录 `README.md` 的“新对话阅读顺序”读取相关文档。
- 已批准决策以 `docs/decisions/README.md` 为准；调研记录和路线图不得替代决策记录。
- 如果新信息推翻现有决策，先更新决策状态和理由，再修改架构或代码。

## 当前阶段

- 项目目前处于设计与知识沉淀阶段。在首版威胁模型、协议和模块边界评审完成前，不创建业务代码脚手架。
- 不为了展示进度提前引入框架、构建系统、数据库、管理后台或多语言 SDK。
- 文档中的事实、推论、建议和已批准决策必须明确区分。

## 工程与安全原则

- 验证码是纵深防御的一层，不宣称单独证明用户一定是人，也不替代限流、MFA 和业务风控。
- 客户端、轨迹、坐标、时间戳和成功回调均是不可信输入。
- 服务端二次校验、挑战与票据一次性消费、场景绑定、过期和重放防护属于核心能力，不转嫁给接入方自行拼装。
- 默认隐私最小化，不采集设备指纹；未来相关能力只能以明确启用的可选插件提供。
- 核心模块不依赖 Spring；Spring Boot Starter 和独立服务是适配层。
- 生产状态存储首先支持 Redis；内存实现只用于本地开发与自动化测试。
- 核心安全边界、状态流转、并发控制和坐标换算必须有自动化测试。
- 不使用未经确认许可的图片、字体、模板、轨迹样本或数据集。

## 改动约束

- 保持最小充分改动，不擅自扩大首版范围。
- 安全协议、公开 API、存储格式或许可证变化必须记录决策。
- 新增依赖前说明用途、许可证、维护状态和替代方案。
- 每项代码任务必须执行与风险匹配的测试、静态检查和构建验证。

